

const HomeCard = ({data}) => {
    const {category, title, card_bg_color, category_bg_color, text_color} = data;

    return (
        <div>
            home crd
        </div>
    );
};

export default HomeCard;